{
  "name": "split-app-functions",
  "version": "1.0.0",
  "description": "Cloud Functions for Expense Split App",
  "main": "lib/index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "build": "tsc"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.5.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "typescript": "^5.1.0",
    "@types/node": "^20.3.0"
  },
  "engines": {
    "node": "20"
  }
}
